# Handoff, 2026-09-12: where the toolkit stands and what happens next

Michael Van Geertruy, with Claude. Project Revive Solutions, LLC.

This is the note to read first when work resumes. It says what is committed, what is built but not committed, what to run, what was decided today, and what comes next. The documents it points to live in `MADTPackage\library` and are mirrored into the tools.

## 1. Where everything is

All toolkit trees moved today to `C:\workspace\MikeVan's AI Development Toolkit\`: `complexity`, `Witness`, `UntangleIt`, `DeepTest`, `MADTPackage`, and `keepsafe`. The HelloWorlds ports stay at `C:\workspace\HelloWorlds`. The release script, the fixture sync, and the documents all point at the new root.

The versioning schema is Major.Minor.Patch, and only the patch number moves, on every delivery, across every tree together. The trees are at 1.0.8 (committed) and the next delivery is 1.0.9. Major and minor move only when Michael says so; the 1.0 label was Claude's mistake, kept because unwinding it cost more than living with it, and it is not a statement that anything is done. The Marketplace still shows DeepTest 0.4.5, UntangleIt 0.1.11, the pack 0.1.6, and KeepSafe 0.1.1; nothing since has been uploaded and nothing goes up until the series ends.

## 2. Committed and pushed (1.0.8, all four trees)

- complexity `ba33d81`, UntangleIt `5b31d32`, DeepTest `92dca7a`, MADTPackage `caa082c`, subject "Playwright without the import line; 1.0 slot done". (The subject's last clause is wrong by the schema above; the history stays as it is.)
- What 1.0.8 holds: Playwright component tests attributed per test with nothing added to a project (a resolve hook in Playwright's workers answers a spec's import of the component package with the Witness fixture); READMEs and setup notes that say Witness instruments in memory and writes nothing into a project; UntangleIt's test gate driving Mocha and Playwright; `MADTPackage\library` with every toolkit document, the byline "Michael Van Geertruy, with Claude" on each, and `library\README.md` as the catalogue; `release.ps1` mirroring the library into the tools and checking every mirror by SHA256.
- HelloWorlds: the react-playwright-ct specs import `@playwright/experimental-ct-react` again and the port commits no `.deeptest` folder (committed by Michael).

## 3. Built and not committed (1.0.9, in the working trees)

Witness is its own library. Nothing measured changes; the Linux harness gives the 1.0.8 numbers to the line on both ports.

- `Witness\`: `@projectrevivesolutions/witness`, `src\instrument.ts`, `src\hook.ts`, `src\treeSitter.ts`, `src\index.ts` (exports `createInstrumenter`, `hooksDir`, `wasmDir`, `HOOK_FILES`, `ENV`, `nodeSupportsWitness`), `hooks\` (witness.cjs, witness-loader.mjs, witness-vite.mjs, witness-playwright-loader.mjs, witness-playwright.template.ts, mocha.cjs; they read `WITNESS_*` environment names now), `vendor\` (three grammars), `esbuild.mjs` (bundles the instrumenter to `dist\hooks\witness-instrument.cjs`, copies hooks and grammars), `test\witness.test.ts` (7 tests), README, CHANGELOG, LICENSE, `docs\witness.md` (mirror). It is not a git repository yet.
- DeepTest: `src\witness\` and the six hooks removed; `coverage.ts` imports from the package; `prepareWorkDir` copies the package's hooks beside DeepTest's own; drivers set `WITNESS_*` beside `DEEPTEST_*`; `esbuild.mjs` copies the package's `dist\hooks` into `dist\hooks`; `package.json` adds `file:../Witness`; `test\witness.test.ts` keeps the differential, the universe, and the Mocha and Playwright driver tests (200 tests in all); `scripts\sync-fixtures.mjs` finds HelloWorlds one level higher; CHANGELOG, README, engineering notes, and build status carry 1.0.9.
- UntangleIt: declares `file:../Witness` (unused until the gate); CHANGELOG.
- MADTPackage: `release.ps1` root is the new path, five trees with Witness second, `witness.md` mirrored into `Witness\docs`; CHANGELOG and README; library documents updated (witness.md draft 4, toolkit-api.md names the second library, architecture and paths).

To run, in order (Witness first, because DeepTest and UntangleIt link to it and its `prepare` builds it):

```powershell
cd "C:\workspace\MikeVan's AI Development Toolkit\Witness"
npm install
npm test
cd "C:\workspace\MikeVan's AI Development Toolkit\DeepTest"
npm install
cd "C:\workspace\MikeVan's AI Development Toolkit\UntangleIt"
npm install
cd "C:\workspace\MikeVan's AI Development Toolkit\MADTPackage"
.\scripts\release.ps1 -Version 1.0.9
```

Expect Witness 7, complexity 40, UntangleIt 16, DeepTest 200, and "library: every mirror matches." first. Then in VS Code: on `HelloWorlds\react-playwright-ct`, Check the tests passes 10, `greet.ts` shows 4(6) 5(1) 7(6), `pickGreeting` first at 27/73/97; on `HelloWorlds\node-mocha`, 10 pass and `pickGreeting` first at 27/73/97.

Before the commit run: `Witness` needs `git init` on `master` and an `origin` at `github.com/mikevan/Witness`. Claude has not verified `gh` on the machine and will not write the repository line until Michael pastes `gh --version`. Then `release.ps1 -Version 1.0.9 -Commit -Subject "Witness is its own library" -BodyFile ...` with the engineering reason from the 1.0.9 section of the engineering notes.

## 4. Decided today

- Witness is a separate library now, not when UntangleIt first needs it, because a shared language between two tools has to be something both import.
- The rest of the JavaScript slot is not done. Remaining, in order: 1.0.10 Witness under Vitest (the existing Vite plugin through the generated runner config; Angular's Vitest builder rides it), 1.0.11 Witness under Jest (a Jest transformer), 1.0.12 Witness under Karma (a preprocessor in the generated config; removes the last coverage package DeepTest depends on), 1.0.13 UntangleIt on Witness (the fingerprint gate). Michael says 1.0.13 is harder than Claude knows; the mapping of an untangled method's new pieces back to the original is the piece Claude must design and put in front of him before code, and he has more to say about it.
- Java waits until the slot is done.
- The library is the one source for documents; the tools carry mirrors; a document is edited in the library and nowhere else.
- Claude is named on every document. Commits carry no attribution trailers. Both are Michael's calls.

## 5. Rules learned the hard way today

- Claude turned a line in its own notes into a decision of Michael's and built nine releases on it (the 1.0 numbering). From now on anything with Michael's name on it that Claude cannot point to him saying in the conversation gets asked before it is acted on: versions, names, scope, what counts as done.
- Every command block is checked against the machine first, one path, no fallbacks. Claude's git status checks leave a stale `.git\index.lock` behind in the tree it looked at; Claude removes it before handing over commands, and does not run status again after that.
- Nothing is committed until it builds and works on the device.

## 6. The harness (Claude's side, Linux VM)

`$HOME\wtest` is a Linux install of the Witness tree (tests run there). `$HOME\dtest` compiles DeepTest's sources with `tsconfig.test.json` into `out\DeepTest`, whose `node_modules` is a symlink farm (DeepTest's packages, a `vitest` shim over `node:test`, a Linux esbuild, `@projectrevivesolutions/witness` to `wtest`, `@projectrevivesolutions/complexity` to the real tree). `out\DeepTest\survey.js <port>` drives a HelloWorlds port copy under `$HOME\hw` and prints what the panel would show. Rebuild with `tsc -p tsconfig.test.json` then `bash fixdyn.sh`. The Jest and Vitest end-to-end tests fail there only because those fixtures have no Linux install; they pass on Michael's machine.

## 7. Documents

`MADTPackage\library\README.md` is the catalogue. Read `toolkit\witness.md` (draft 4) before touching instrumentation, `deeptest\engineering-notes.md` (through 1.0.9) before changing anything that looks odd, and `toolkit\toolkit-api.md` for the contract between tools and the two libraries. The claude.ai project holds convenience copies; the library is authoritative.
